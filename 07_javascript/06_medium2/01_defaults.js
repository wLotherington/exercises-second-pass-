// The limitation of both functions is that an ommitted argument will not reserve the variable space.
// if fewer arguments are passed to the function than expected, the last ones will always be the ones
// assumed to be missing