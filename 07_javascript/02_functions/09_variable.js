console.log(a);

var a = 1;

// undefined
// the variable declaration of 'a' will be hoisted to the top of the global scope
// but the variable assignment to the value 1 will not. therefore, no error, but
// undefined